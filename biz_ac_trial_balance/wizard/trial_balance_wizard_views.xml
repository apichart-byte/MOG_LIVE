<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_trial_balance_wizard_form" model="ir.ui.view">
        <field name="name">biz.trial.balance.wizard.form</field>
        <field name="model">biz.trial.balance.wizard</field>
        <field name="arch" type="xml">
            <form string="งบทดลอง">
                <group>
                    <group string="ช่วงเวลา">
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="company_ids" widget="many2many_tags"
                               options="{'no_create': True}"/>
                        <field name="is_multi_company" invisible="1"/>
                        <field name="company_mode" invisible="not is_multi_company"/>
                        <field name="date_format" widget="radio" options="{'horizontal': True}"/>
                    </group>
                    <group string="เกณฑ์รายงาน">
                        <field name="closing_mode"/>
                        <field name="opening_basis"/>
                        <field name="target_move"/>
                        <field name="compare"/>
                    </group>
                </group>
                <group>
                    <group string="การแสดงผล">
                        <field name="display_account"/>
                        <field name="group_by_type"/>
                    </group>
                    <group string="ตัวกรองเพิ่มเติม">
                        <field name="journal_ids" widget="many2many_tags"
                               options="{'no_create': True}"/>
                        <field name="analytic_account_ids" widget="many2many_tags"
                               options="{'no_create': True}"/>
                        <field name="partner_ids" widget="many2many_tags"
                               options="{'no_create': True}"/>
                    </group>
                </group>
                <field name="xlsx_file" invisible="1"/>
                <field name="xlsx_filename" invisible="1"/>
                <footer>
                    <button name="action_view_report" string="ดูบนหน้าจอ"
                            type="object" class="btn-primary"/>
                    <button name="action_print_pdf" string="พิมพ์ PDF"
                            type="object" class="btn-secondary"/>
                    <button name="action_export_xlsx" string="ดาวน์โหลด Excel"
                            type="object" class="btn-secondary"/>
                    <button string="ยกเลิก" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_trial_balance_wizard" model="ir.actions.act_window">
        <field name="name">พิมพ์งบทดลอง</field>
        <field name="res_model">biz.trial.balance.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_trial_balance_wizard_form"/>
        <field name="target">new</field>
        <field name="groups_id" eval="[(4, ref('group_trial_balance_user'))]"/>
    </record>

</odoo>
