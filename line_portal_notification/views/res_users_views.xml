<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add LINE User ID field to user form -->
    <record id="view_users_form_line_user_id" model="ir.ui.view">
        <field name="name">res.users.form.line.user.id</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='signature']" position="after">
                <field name="line_user_id" string="LINE User ID" placeholder="e.g., Ua1b2c3d4e5f6..."/>
            </xpath>
        </field>
    </record>

    <!-- Add LINE User ID to partner form -->
    <record id="view_partner_form_line_user_id" model="ir.ui.view">
        <field name="name">res.partner.form.line.user.id</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='phone']" position="after">
                <field name="line_user_id" placeholder="LINE User ID"/>
            </xpath>
        </field>
    </record>
</odoo>
