<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_mrp_unbuild_form_buz_guard" model="ir.ui.view">
            <field name="name">mrp.unbuild.form.buz.guard</field>
            <field name="model">mrp.unbuild</field>
            <field name="inherit_id" ref="mrp.mrp_unbuild_form_view"/>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <xpath expr="//header" position="replace">
                    <header>
                        <button name="action_create_picking"
                                type="object"
                                string="Create Picking"
                                class="oe_highlight"
                                invisible="state != 'draft'"/>
                        <button name="action_cancel_picking"
                                type="object"
                                string="Cancel Picking"
                                invisible="state != 'picking'"/>
                        <button name="action_view_picking"
                                type="object"
                                string="Open Picking"
                                invisible="not picking_id"/>
                        <button name="action_confirm_done"
                                type="object"
                                string="Confirm Done"
                                class="oe_highlight"
                                invisible="state != 'confirm'"/>
                        <button name="%(buz_unbuild_state_guard.report_unbuild_job)d"
                                type="action"
                                string="Print"
                                class="oe_highlight"
                                groups="mrp.group_mrp_user"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,picking,confirm,done"/>
                    </header>
                </xpath>
                <xpath expr="//sheet/div[@name='button_box']" position="inside">
                    <button name="action_view_picking"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-truck"
                            invisible="not picking_id">
                        <field name="picking_count" widget="statinfo" string="Picking"/>
                    </button>
                </xpath>
                <xpath expr="//field[@name='company_id']" position="after">
                    <field name="picking_id" readonly="1"/>
                </xpath>
            </field>
        </record>

        <record id="view_mrp_unbuild_search_buz_guard" model="ir.ui.view">
            <field name="name">mrp.unbuild.search.buz.guard</field>
            <field name="model">mrp.unbuild</field>
            <field name="inherit_id" ref="mrp.mrp_unbuild_search_view"/>
            <field name="priority">100</field>
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <field name="picking_id"/>
                    <filter string="Picking" name="picking" domain="[('state', '=', 'picking')]"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
