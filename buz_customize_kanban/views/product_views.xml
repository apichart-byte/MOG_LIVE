<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_kanban_view_inherit" model="ir.ui.view">
        <field name="name">product.template.kanban.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_kanban_view"/>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="inside">
                <field name="product_tag_ids"/>
                <field name="qty_available"/>
                <field name="incoming_qty"/>
                <field name="outgoing_qty"/>
                <field name="kanban_available_qty"/>
                <field name="kanban_price"/>
            </xpath>
            <xpath expr="//div[hasclass('o_kanban_record_top')]" position="replace">
                <div class="o_kanban_record_top">
                    <div class="o_kanban_record_headings">
                        <strong class="o_kanban_record_title">
                            <field name="name"/>
                        </strong>
                        <small class="o_kanban_record_subtitle text-muted">
                            <field name="name_eng"/>
                        </small>
                    </div>
                    <div class="o_dropdown_kanban dropdown">
                        <a class="dropdown-toggle o-no-caret btn" role="button" data-bs-toggle="dropdown" data-bs-display="static" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                            <span class="fa fa-ellipsis-v"/>
                        </a>
                        <div class="dropdown-menu" role="menu">
                            <t t-if="widget.editable">
                                <a role="menuitem" type="edit" class="dropdown-item">Edit</a>
                            </t>
                            <t t-if="widget.deletable">
                                <a role="menuitem" type="delete" class="dropdown-item">Delete</a>
                            </t>
                        </div>
                    </div>
                </div>
            </xpath>
            <field name="list_price" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            <field name="list_price" position="after">
                <field name="kanban_price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                <!-- Stock Information -->
                <div class="mt-2">
                    <div class="d-flex gap-1">
                        <span><strong>Available:</strong></span>
                        <span><strong><field name="kanban_available_qty" widget="float" options="{'digits': [16,2]}"/> <field name="uom_id"/></strong></span>
                    </div>
                </div>
                <!-- Product tags -->
                <div class="mt-2">
                    <field name="product_tag_ids" widget="many2many_tags"/>
                </div>
            </field>
        </field>
    </record>

    <!-- Inherit product search view to add warehouse filter -->
    <record id="product_template_search_view_inherit_warehouse" model="ir.ui.view">
        <field name="name">product.template.search.warehouse.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <field name="warehouse_id" string="Warehouse"/>
                <separator/>
                <filter name="filter_in_warehouse" string="Has Stock in Warehouse"
                        domain="[('warehouse_id', '!=', False)]"/>
            </xpath>
        </field>
    </record>
</odoo>
