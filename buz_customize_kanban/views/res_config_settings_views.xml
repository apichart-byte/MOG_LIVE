<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_inherit" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.kanban</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='warehouse_setting_container']" position="after">
                <block title="Kanban Display" name="kanban_display_setting_container">
                    <setting id="kanban_warehouse_filter" string="Kanban Warehouse" help="Select warehouse to display available quantity in product kanban view">
                        <div class="content-group">
                            <div class="mt16">
                                <field name="kanban_warehouse_id" 
                                       placeholder="Select warehouse to display in kanban..."/>
                            </div>
                            <div class="text-muted mt8">
                                <i class="fa fa-info-circle"/> If no warehouse is selected, default warehouse will be used
                            </div>
                        </div>
                    </setting>
                    <setting id="kanban_pricelist_filter" string="Kanban Pricelist" help="Select pricelist to display product price in kanban view">
                        <div class="content-group">
                            <div class="mt16">
                                <field name="kanban_pricelist_id" 
                                       placeholder="Select pricelist to display in kanban..."/>
                            </div>
                            <div class="text-muted mt8">
                                <i class="fa fa-info-circle"/> If no pricelist is selected, the default sales price will be shown
                            </div>
                        </div>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
